'use strict';

const person = 2;
amountOfBeer(person);

function amountOfBeer(person) {
    for (var i = 0; i <= 4; i++) {
        var j = i;
    }
    var i = person;
    console.log('The result is:', i + j);
}